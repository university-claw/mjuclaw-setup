{
  "name": "mjuclaw-setup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p src/tsconfig.json",
    "test": "npm run build && node --test \"test/**/*.test.cjs\"",
    "start": "node dist/view-server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "isomorphic-dompurify": "^2.24.0",
    "marked": "^16.4.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
